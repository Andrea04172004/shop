spring.jpa.show-sql=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.url=jdbc:mysql://localhost:3306/online_shop_new?useSSL=false&serverTimezone=UTC
spring.jpa.hibernate.ddl-auto=create-drop
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL57Dialect

secret = kachmar
expiration = 70000

queries.user-query = select email, password, active from users where email = ?;
queries.role-query = select email, role from users where email = ?;

